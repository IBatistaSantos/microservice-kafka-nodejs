# Micro-service with Node.js and Kafka


Development a REST API in Node.js that integrates with a micro-service using Apache Kafka.

## Installation

From the main directory, execute the statement. This instruction up the docker container

```bash
docker compose up
```
In the API directory, execute the following statement, do the same in the certfication directory

```bash
yarn dev 
```

